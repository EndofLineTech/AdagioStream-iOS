name: AdagioStream
options:
  bundleIdPrefix: com.adagiostream
  deploymentTarget:
    iOS: "16.0"
  xcodeVersion: "15.0"
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: "5.9"
    TARGETED_DEVICE_FAMILY: "1,2"

packages:
  VLCKitSPM:
    url: https://github.com/tylerjonesio/vlckit-spm
    from: "3.6.0"

targets:
  AdagioStream:
    type: application
    platform: iOS
    sources:
      - AdagioStream
    dependencies:
      - package: VLCKitSPM
    info:
      path: AdagioStream/Info.plist
      properties:
        ITSAppUsesNonExemptEncryption: false
        UIBackgroundModes: [audio]
        CFBundleDisplayName: Adagio Stream
        NSSupportsLiveActivities: false
        UILaunchScreen:
          UIColorName: LaunchBackground
          UIImageName: ""
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
          UISceneConfigurations:
            UIWindowSceneSessionRoleApplication:
              - UISceneConfigurationName: Default Configuration
            CPTemplateApplicationSceneSessionRoleApplication:
              - UISceneConfigurationName: CarPlay Configuration
                UISceneDelegateClassName: $(PRODUCT_MODULE_NAME).CarPlaySceneDelegate
    settings:
      base:
        CODE_SIGN_ENTITLEMENTS: AdagioStream/AdagioStream.entitlements
        PRODUCT_BUNDLE_IDENTIFIER: com.adagiostream.app
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "30"
        DEVELOPMENT_TEAM: EWT2X7Q9ZJ
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: true
        INFOPLIST_KEY_UIRequiresFullScreen: true
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.music
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: UIInterfaceOrientationPortrait
    entitlements:
      path: AdagioStream/AdagioStream.entitlements
      properties:
        com.apple.developer.carplay-audio: true

  AdagioStreamTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - AdagioStreamTests
    dependencies:
      - target: AdagioStream
    settings:
      base:
        GENERATE_INFOPLIST_FILE: true
